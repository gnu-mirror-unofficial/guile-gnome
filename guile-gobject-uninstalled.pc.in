# the standard variables don't make sense for an uninstalled copy
prefix=
exec_prefix=
libdir=${pcfiledir}
includedir=${pcfiledir}
have_gtk=true # fix this later...

Name: (guile gobject), Uninstalled
Description: Guile bindings for GObject and Gtk, Not Installed
Version: @VERSION@
Requires: gobject-2.0
Libs: ${libdir}/gnome/gobject/libguile-gnome-gw-gobject.la
Cflags: -I${includedir}/gnome/gobject
