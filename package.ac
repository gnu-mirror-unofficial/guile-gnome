PKG_CHECK_MODULES(GNOME_VFS, gnome-vfs, HAVE_GNOME_VFS=true, 
                                      HAVE_GNOME_VFS=false)
AC_SUBST(GNOME_VFS_CFLAGS)
AC_SUBST(GNOME_VFS_LIBS)
AM_CONDITIONAL(HAVE_GNOME_VFS, $HAVE_GNOME_VFS)
