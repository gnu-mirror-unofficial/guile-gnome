
@c %start of fragment

@deftp Class <gnome-scores>
This @code{<gobject>} class defines no properties, other than those defined by
its superclasses.

@end deftp

@deffn Function gnome-scores-display-with-pixmap  (pixmap_logo@tie{}@code{mchars}) (app_name@tie{}@code{mchars}) (level@tie{}@code{mchars}) (pos@tie{}@code{int}) @result{}@tie{} (ret@tie{}@code{<gtk-widget>})
@samp{gnome_scores_display_with_pixmap} is deprecated and should not be used in
newly-written code.

Does all the work of displaying the best scores. It calls
gnome_score_get_notables to retrieve the info, creates the window, and show it.

@table @var
@item pixmap-logo
Filename of a logo pixmap to display

@item app-name
Name of the application, as in gnome_score_init.

@item level
Level of the game or @samp{@code{#f}}.

@item pos
Position in the top ten of the current player, as returned by gnome_score_log.

@item ret
If a dialog is displayed return it's pointer. It can also be @samp{@code{#f}} if
no dialog is displayed

@end table

@end deffn

@deffn Function gnome-scores-new  (n_scores@tie{}@code{unsigned-int}) (clear@tie{}@code{bool}) @result{}@tie{} (ret@tie{}@code{<gtk-widget>}) (names@tie{}@code{mchars}) (scores@tie{}@code{float}) (times@tie{}@code{long})
@samp{gnome_scores_new} is deprecated and should not be used in newly-written
code.

Creates the high-scores window.

@table @var
@item n-scores
Number of positions.

@item names
Names of the players.

@item scores
Scores

@item times
Time in which the scores were done

@item clear
Add a "Clear" Button?

@item ret
A new @code{<gnome-scores>} widget

@end table

@end deffn

@deffn Function gnome-scores-construct  (self@tie{}@code{<gnome-scores>}) (n_scores@tie{}@code{unsigned-int}) (clear@tie{}@code{bool}) @result{}@tie{} (names@tie{}@code{mchars}) (scores@tie{}@code{float}) (times@tie{}@code{long})
@deffnx Method construct
@samp{gnome_scores_construct} is deprecated and should not be used in
newly-written code.

useful for language bindings and subclassing, not to be used by itself, see
@code{<gnome-scores-new>}.

@table @var
@item gs


@item n-scores
Number of positions.

@item names
Names of the players.

@item scores
Scores

@item times
Time in which the scores were done

@item clear
Add a "Clear" Button?

@end table

@end deffn

@deffn Function gnome-scores-set-logo-label  (self@tie{}@code{<gnome-scores>}) (txt@tie{}@code{mchars}) (font@tie{}@code{mchars}) (col@tie{}@code{<gdk-color>})
@deffnx Method set-logo-label
@samp{gnome_scores_set_logo_label} is deprecated and should not be used in
newly-written code.

Creates a label to be the logo

@table @var
@item gs
A @code{<gnome-scores>} widget

@item txt
Text in the label.

@item font
Font to use in the label.

@item col
Color to use in the label.

@end table

@end deffn

@deffn Function gnome-scores-set-logo-pixmap  (self@tie{}@code{<gnome-scores>}) (pix_name@tie{}@code{mchars})
@deffnx Method set-logo-pixmap
@samp{gnome_scores_set_logo_pixmap} is deprecated and should not be used in
newly-written code.

Sets the logo on the scores dialog box to a pixmap

@table @var
@item gs
A @code{<gnome-scores>} widget

@item pix-name
filename of a pixmap

@end table

@end deffn

@deffn Function gnome-scores-set-logo-widget  (self@tie{}@code{<gnome-scores>}) (w@tie{}@code{<gtk-widget>})
@deffnx Method set-logo-widget
@samp{gnome_scores_set_logo_widget} is deprecated and should not be used in
newly-written code.

Set an arbitrary widget to be the logo.

@table @var
@item gs
A @code{<gnome-scores>} widget

@item w
Widget to be used as logo.

@end table

@end deffn

@deffn Function gnome-scores-set-color  (self@tie{}@code{<gnome-scores>}) (n@tie{}@code{unsigned-int}) (col@tie{}@code{<gdk-color>})
@deffnx Method set-color
@samp{gnome_scores_set_color} is deprecated and should not be used in
newly-written code.

Set the color of one entry.

@table @var
@item gs
A @code{<gnome-scores>} widget

@item n
Entry to be changed.

@item col
Color.

@end table

@end deffn

@deffn Function gnome-scores-set-def-color  (self@tie{}@code{<gnome-scores>}) (col@tie{}@code{<gdk-color>})
@deffnx Method set-def-color
@samp{gnome_scores_set_def_color} is deprecated and should not be used in
newly-written code.

Set the default color of the entries.

@table @var
@item gs
A @code{<gnome-scores>} widget

@item col
Color

@end table

@end deffn

@deffn Function gnome-scores-set-colors  (self@tie{}@code{<gnome-scores>}) (col@tie{}@code{<gdk-color>})
@deffnx Method set-colors
@samp{gnome_scores_set_colors} is deprecated and should not be used in
newly-written code.

Set the color of all the entries.

@table @var
@item gs
A @code{<gnome-scores>} widget

@item col
Array of colors.

@end table

@end deffn

@deffn Function gnome-scores-set-logo-label-title  (self@tie{}@code{<gnome-scores>}) (txt@tie{}@code{mchars})
@deffnx Method set-logo-label-title
@samp{gnome_scores_set_logo_label_title} is deprecated and should not be used in
newly-written code.

Creates a label to be the logo

@table @var
@item gs
A @code{<gnome-scores>} widget

@item txt
Name of the logo.

@end table

@end deffn

@deffn Function gnome-scores-set-current-player  (self@tie{}@code{<gnome-scores>}) (i@tie{}@code{int})
@deffnx Method set-current-player
@samp{gnome_scores_set_current_player} is deprecated and should not be used in
newly-written code.

Set the index of the current player in top ten.

@table @var
@item gs
A @code{<gnome-scores>} widget

@item i
Index of the current(from 0 to 9).

@end table

@end deffn


@c %end of fragment
